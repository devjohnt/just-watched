<% include ../partials/header %>

<div class="container" style="{text-align: center;}">
    <h3>Diary Page of <%= currentUser.username %></h3>
    
    <table class="table-hover">
        <thead>
            <tr>
                <th>Month</th>
                <th>Day</th>
                <th>Movie</th>
                <th>Released</th>
                <th>Rating</th>
                <th>Like</th>
                <th>Rewatch</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            
            <!-- For every movie! -->
            <% movies.forEach(function(diary) { %>
            <tr>
                <td><%= diary.watchDate.toLocaleString('en-GB', {month: 'long'}) %></td>
                <td><%= diary.watchDate.toLocaleString('en-GB', {day: '2-digit'}) %></td>
                <td><%= diary.movie[0].Title %></td>
                <td><%= diary.movie[0].Released %></td>
                <% var ratings = diary.movie[0].Ratings; %>
                <td>
                    <% ratings.forEach(function(rating) { %>
                        <%= rating.Source %>: <%= rating.Value %>
                    <% }); %>
                </td>
                <td>22 likes</td>
                <td>
                    <% if (diary.isRewatched) { %>
                        Many times!
                    <% } else { %>
                        First Time!
                    <% }  %>
                </td>
                <td>
                    <form action="diary/<%= diary._id %>/edit" method="GET">
                        <input type="hidden" value="<%= diary.movie[0].imdbID %>" name="imdbID">
                        <input class="btn btn-primary btn-sm" type="submit" value="Edit">
                    </form>
                </td>
            </tr>
            <% }); %>

        </tbody>
    </table>
</div>



<% include ../partials/footer %>