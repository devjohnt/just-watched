<% include ../partials/header %>

<div class="container" style="{text-align: center;}">
    <h3>Diary Page of <%= currentUser.username %></h3>
    
    <table class="table-hover">
        <thead>
            <tr>
                <th>Month</th>
                <th>Day</th>
                <th>Movie</th>
                <th>Released</th>
                <th>Rating</th>
                <th>Like</th>
                <th>Rewatch</th>
            </tr>
        </thead>
        <tbody>
            
            <!-- For every movie! -->
            <% movies.forEach(function(movie) { %>
            <tr>
                <td><%= movie.watchDate.toLocaleString('en-GB', {month: 'long'}) %></td>
                <td><%= movie.watchDate.toLocaleString('en-GB', {day: '2-digit'}) %></td>
                <td><%= movie.movie[0].Title %></td>
                <td><%= movie.movie[0].Released %></td>
                <% var ratings = movie.movie[0].Ratings; %>
                <td>
                    <% ratings.forEach(function(rating) { %>
                        <%= rating.Source %>: <%= rating.Value %>
                    <% }); %>
                </td>
                <td>22 likes</td>
                <td>
                    <% if (movie.isRewatched) { %>
                        Many times!
                    <% } else { %>
                        First Time!
                    <% }  %>
                </td>
            </tr>
            <% }); %>

        </tbody>
    </table>
</div>



<% include ../partials/footer %>